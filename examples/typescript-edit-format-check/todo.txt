It looks like this one invented some kind of syntax that's not referenced anywhere, and I don't like it. I wanted to use something like Zod to define the schema. Not this:

output_schema: "{files_modified: array<string>}"