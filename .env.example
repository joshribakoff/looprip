# Copy this file to .env and populate with your API credentials before running the agent loop.
OPENAI_API_KEY=sk-openai-key
ANTHROPIC_API_KEY=anthropic-api-key

# Select which model provider to use: openai | anthropic
AGENT_PROVIDER=openai

# Optional overrides for model choices
OPENAI_MODEL=gpt-4o-mini
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Runtime tuning
AGENT_MAX_ITERATIONS=2
AGENT_USER_PROMPT=Create hello-world.ts with some fun random ascii art generation logic.
