---
const title = 'Agent Pipeline Docs (Astro preview)';
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <style>
      :root { --bg: #0b1220; --fg: #e6edf3; --muted: #9fb1c6; --accent: #44d860; }
      html, body { height: 100%; }
      body { margin: 0; background: var(--bg); color: var(--fg); font: 16px/1.6 system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji"; }
      .wrap { min-height: 100%; display: grid; place-items: center; padding: 3rem 1rem; }
      .card { max-width: 760px; width: 100%; border: 1px solid #1f2a44; border-radius: 12px; padding: 2rem; background: #0f1629; }
      .logo { width: 64px; height: 64px; border-radius: 8px; display: inline-grid; place-items: center; background: #0b1220; border: 1px solid #1f2a44; }
      h1 { margin: 1rem 0 0.5rem; font-size: 1.75rem; }
      p { margin: 0.5rem 0 1rem; color: var(--muted); }
      .actions { display: flex; gap: 0.75rem; flex-wrap: wrap; }
      a.btn { text-decoration: none; color: var(--bg); background: var(--accent); padding: 0.6rem 0.9rem; border-radius: 8px; font-weight: 600; }
      a.secondary { background: transparent; border: 1px solid #2b3b5f; color: var(--fg); }
      code { background: #101a33; padding: 0.15rem 0.35rem; border-radius: 6px; border: 1px solid #1f2a44; }
    </style>
  </head>
  <body>
    <main class="wrap">
      <section class="card">
        <div class="logo">
          <img src="/img/logo.svg" alt="Logo" width="36" height="36" />
        </div>
        <h1>{title}</h1>
        <p>This is a fresh Astro site living in <code>/docs-astro</code>. We'll migrate the existing Docusaurus content here incrementally.</p>
        <div class="actions">
          <a class="btn" href="/">Home</a>
          <a class="btn secondary" href="https://docs.astro.build/" target="_blank" rel="noreferrer">Astro Docs ↗</a>
        </div>
      </section>
    </main>
  </body>
</html>
