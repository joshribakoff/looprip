These pages could go a lot further as far as showing how our tool solves it with the example of YAML. It should be as simple as possible than the command that the user can run to replicate the solution. 