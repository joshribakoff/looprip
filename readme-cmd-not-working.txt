The command in the Readme does not work. It just says "agent-pipeline" not found. 

tsx src/cli/index.js

That does work fine. I think we should keep things simple. Can we just put like a simple npm script or a bash script at the top of the repo for now? 